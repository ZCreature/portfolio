# Tool thumbnails

Drop screenshots here with these exact names and they get picked up:

    trackdash.png      TrackDash — CarPlay / OBD2 dashboard
    asciicam.png       ASCII Glitch Camera — iOS
    mybeats.png        MyBEATS — optional, replaces the design SVG

Already present:

    color-safety-lens.png   captured from the live tool
    diskmap.png             real app capture
    mybeats-ui.svg          the project's own design file

Notes:
- Landscape suits the 16:10 tile; portrait phone shots get cropped to the
  top-left, so leave headroom or say and I'll switch that tile to `contain`.
- Anything wider than about 1600px is downscaled — `sips -Z 1600 file.png`.
- Keep them under ~500 KB each; the whole site is currently under 1 MB of
  images.
